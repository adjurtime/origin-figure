interface:
  display_name: "Origin Figure"
  short_description: "Create concise, editable paper figures in Origin"
  default_prompt: "Use $origin-figure to create or revise an editable, Word-sized paper figure in Origin and export only when I request it."
